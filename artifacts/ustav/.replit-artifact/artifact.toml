kind = "web"
previewPath = "/"
title = "USTAV - Invitation Card Platform"
version = "1.0.0"
id = "artifacts/ustav"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 18348

[services.development]
run = "pnpm --filter @workspace/ustav run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/ustav", "run", "build" ]
publicDir = "artifacts/ustav/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "18348"
BASE_PATH = "/"
